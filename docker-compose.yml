services:
  kids-jukebox:
    image: ghcr.io/swivelingtoast/kids-jukebox:latest
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - jukebox-data:/app/backend/src/data

volumes:
  jukebox-data:
